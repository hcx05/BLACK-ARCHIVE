//OCPA REGION 4 - INTERNAL USE ONLY//
FROM: T. Reyes (Systems)
TO: Self / Systems Backlog
DATE: 2547-02-09
RE: Open items

- [x] Fix upload validation on Case File Intake — added an image check
      (SEC-1188) so it at least rejects garbage files. Extension
      allowlist/rename still open, marked low priority, don't re-open.
- [ ] Escape the search query field, for real this time
- [ ] Pull the /debug endpoint off the webmail box. Should've done this
      after the audit last year.
- [ ] Rotate the sandbox DB password. Again.
- [ ] Ask records why three "closed" dependent cases from different
      colonies are the only closed cases in the whole index still
      carrying anything in the internal transfer-reference field.
      Every other closed case has it blank, same as the active ones.
      Probably nothing. Probably a batch import artifact from the
      SPINDLE days.
- [ ] Stop asking that last one out loud.
