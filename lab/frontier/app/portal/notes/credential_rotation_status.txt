//OCPA REGION 4 - INTERNAL USE ONLY//
FROM: T. Reyes (Systems)
TO: Systems Distribution List
DATE: 2547-01-30
RE: Legacy account rotation - status update

Follow-up to the last two audits. Scope here is terminal/webmail
human-login accounts only - machine/service accounts for automated jobs
are tracked separately by Ops, not on this list.

  ACCOUNT          SERVICE              STATUS
  sysadmin         terminal / webmail   default password, NOT rotated
  devuser          terminal / webmail   rotated 2546-11, first-login forced
  deploy           terminal / webmail   decommissioned, account disabled

devuser went through first-login rotation after last year's audit -
whatever's in the old provisioning script for that one is stale, don't
waste time on it. deploy was pulled entirely when that box got
decommissioned.

sysadmin is the one still open. It's flagged as a shared terminal/service
account so Security wanted a heads-up before forcing rotation on it, and
that sign-off has been sitting in someone's queue since January. It has
never gone through first-login rotation, so it's still sitting on
whatever the provisioning template hands out by default (see onboarding
note if you need the current template default).

Re-open if Security ever actually signs off and I can force it.

- T.R.
