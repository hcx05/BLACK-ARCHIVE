//OCPA REGION 4 - INTERNAL USE ONLY//
FROM: T. Reyes (Systems)
TO: New Terminal User
DATE: 2547-02-02
RE: ROSTER onboarding

Welcome to the team. A few things that never made it into the official
onboarding packet:

- The Dependent Status Index is slow to update. If a record looks wrong,
  it's probably just lag from the last SPINDLE migration batch, not an
  actual data error. Don't file a correction ticket for that.
- The search page used to throw unescaped output on bad input. Flagged as
  SEC-1204, closed two cycles ago. If you see it happening again, that's
  a regression - open a new ticket, don't reopen the old one.
- If you need to reach LEDGER for anything cross-referenced with the
  old case files, you'll need duty.admin credentials, not your own.
  Ask around — I'm not supposed to hand those out over a ticket.
- dev DB creds for the sandbox instance, in case Ops asks again:
    host: relay.internal
    user: app_svc
    pass: S3cretDB!2024
  (This is the sandbox copy only. Don't use it against anything live.)
- Every new terminal/webmail account still gets provisioned from the same
  account-setup template until someone forces a rotation on it. Standard
  temp password out of that template is admin123 - if an account you're
  looking at hasn't gone through first-login rotation yet, that's what
  it's still sitting on.

— T.R.
